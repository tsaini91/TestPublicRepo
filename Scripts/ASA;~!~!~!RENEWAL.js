//YAZAN BARGHOUTH: this script is not branched by Accela function doScriptActions(), don't use it

//showMessage = true;
//showDebug = true;
//aa.print("YAZAN -- creating relation " + parentCapId);
//if (parentCapId != "" && parentCapId != null) {
//	var result = aa.cap.createRenewalCap(parentCapId, capId, true);
//
//}
//doConfigurableScriptActions();